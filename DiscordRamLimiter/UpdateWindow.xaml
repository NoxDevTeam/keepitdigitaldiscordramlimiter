<Window x:Class="DiscordRamLimiter.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keep It Digital Discord RAM Limiter Update"
        Width="430"
        Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#121620"
        Foreground="#F4F7FF"
        Closing="Window_Closing">
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Update available"
                   FontSize="25"
                   FontWeight="Bold" />

        <TextBlock x:Name="VersionText"
                   Grid.Row="1"
                   Margin="0,7,0,18"
                   Foreground="#95A0B8"
                   FontSize="13" />

        <Border Grid.Row="2"
                Padding="16"
                CornerRadius="14"
                Background="#1B2231"
                BorderBrush="#2A3448"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ChangelogText"
                           TextWrapping="Wrap"
                           Foreground="#DCE3F5"
                           FontSize="13"
                           LineHeight="20" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <ProgressBar x:Name="DownloadProgressBar"
                         Height="7"
                         Minimum="0"
                         Maximum="100"
                         Visibility="Collapsed" />
            <TextBlock x:Name="DownloadStatusText"
                       Margin="0,7,0,0"
                       Foreground="#95A0B8"
                       FontSize="12"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Margin="0,18,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="LaterButton"
                    Width="92"
                    Height="36"
                    Content="Later"
                    Click="LaterButton_Click" />
            <Button x:Name="InstallButton"
                    Width="160"
                    Height="36"
                    Margin="10,0,0,0"
                    Content="Download and install"
                    Background="#5865F2"
                    Click="InstallButton_Click" />
        </StackPanel>
    </Grid>
</Window>
