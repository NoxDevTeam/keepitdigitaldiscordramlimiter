Added an optional 99% system-memory emergency shutdown guard with a 30-second trigger and cancellable 60-second countdown.
The guard is disabled by default, clearly warns about unsaved work before it can be enabled, and preserves its setting across updates.
